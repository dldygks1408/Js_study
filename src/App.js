import './App.css';

//react는 독자적인 element를 구성한다. (html element가 아니다.)
//react의 element는 html element로 render로 다시 출력되기 때문에 구성이 비슷하다.
//자바스크립트에서 html element를 불러올때 사용하는 이름과 비슷하다.

function App() {
  return (
    <div className="App">
      난 App이야
    </div>
  );
}

export default App;